<Window x:Class="ShoeStoreManager.LoginForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизація" Height="350" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#E2EAF4"
        KeyDown="Window_KeyDown">
    <Grid>
        <StackPanel Margin="20" VerticalAlignment="Center">

            <TextBlock Text="Вхід у систему" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" Width="130"/>

            <TextBlock Text="Логін:" Margin="0,0,0,5"/>
            <TextBox Name="LoginTextBox" Height="25" Margin="0,0,0,10" VerticalContentAlignment="Center"/>

            <TextBlock Text="Пароль:" Margin="0,0,0,5"/>
            <PasswordBox Name="PasswordBox" Height="25" Margin="0,0,0,20" VerticalContentAlignment="Center"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Name="LoginButton" Content="Вхід" Height="30" Grid.Column="0" Margin="0,0,5,0"  Background="#00478E" Foreground="White" BorderBrush="{x:Null}" Click="LoginButton_Click"/>

                <Button Name="CancelButton" Content="Скасувати" Height="30" Grid.Column="1" Margin="5,0,0,0" Background="#00478E" Foreground="White" BorderBrush="{x:Null}" Click="CancelButton_Click"/>
            </Grid>

        </StackPanel>
    </Grid>
</Window>